<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS"
        tools:ignore="SmsAndCallLogPolicy" />
    <uses-feature android:name="android.hardware.telephony" android:required="false"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmsBridge"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <service
            android:name="ro.bitweb.smsbridge.services.WebSocketService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="WebSocket connection for SMS synchronization" />
        </service>

        <!-- Primeste rezultatul REAL de trimitere/livrare SMS de la SmsManager
             (vezi SmsSender.kt / SmsStatusReceiver.kt). Nu e exportat: broadcast-urile
             vin doar din PendingIntent-uri create de aplicatia noastra. -->
        <receiver
            android:name="ro.bitweb.smsbridge.services.SmsStatusReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="ro.bitweb.smsbridge.SMS_SENT" />
                <action android:name="ro.bitweb.smsbridge.SMS_DELIVERED" />
            </intent-filter>
        </receiver>

        <!-- Reporneste WebSocketService dupa reboot, ca sa nu ramana app-ul "mort"
             pana la deschiderea manuala. Vezi BootReceiver.kt. -->
        <receiver
            android:name="ro.bitweb.smsbridge.services.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity
            android:name="ro.bitweb.smsbridge.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SmsBridge">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>